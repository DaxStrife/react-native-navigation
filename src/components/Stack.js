import { Text, View } from "react-native";

const Stack = () => {
  return (
    <View>
      <Text style={{ fontSize: 30, textAlign: "center" }}>Stack Screen</Text>
    </View>
  );
};

export default Stack;
