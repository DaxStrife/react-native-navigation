import { Text, View } from "react-native";

const Settings = () => {
  return (
    <View>
      <Text style={{ fontSize: 30, textAlign: "center" }}>Settings Screen</Text>
    </View>
  );
};

export default Settings;
